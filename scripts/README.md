# AI Pre-commit Hook

Автоматическая проверка кода через ChatGPT перед коммитом.

## Установка

1. Установите переменную окружения с API ключом OpenAI:
   
   export OPENAI_API_KEY='your-api-key-here'
      
   Или добавьте в `.env` файл (если используете):
   